export enum Routes {
  AUTH = 'auth',
  USERS = 'users',
  USER_PROFILE = 'user/profile',
}

export enum Services {
  AUTH = 'AUTH_SERVICE',
  USERS = 'USERS_SERVICE',
  USER_PROFILE = 'USER_PROFILE_SERVICE',
}
